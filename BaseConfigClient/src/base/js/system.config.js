/**
 * Created by dell on 2017/3/22.
 */
define(['jquery'], function (jQuery) {

    return (function (scope, $) {

        // 这里包含一些系统的基本参数, parent参数用于定位父层级对象

        return scope;

    })({}, jQuery);
});