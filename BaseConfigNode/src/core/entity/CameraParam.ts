export class CameraParam {
    // 相机ID
    CameraId: string;
    
    // 最小值
    MinSize: number;

    // 最大值
    MaxSize: number;

    // (相框位置坐标)左
    Left: number;

    // (相框位置坐标)右
    Right: number;

    // (相框位置坐标)上边
    Top: number;

    // (相框位置坐标)下边
    Bottom: number;
}